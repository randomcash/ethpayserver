node_modules/
playwright-report/
test-results/
perf-results.json

# TMPDIR for local runs, see playwright.config.ts — keeps browser profiles
# and Playwright's internal temp files off a RAM-backed /tmp.
.tmp/
